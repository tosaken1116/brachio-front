import { Loader2Icon } from "lucide-react";

export const DrawOmikujiLoadingPresentation = () => {
	return (
		<div className="flex w-full h-full justify-center items-center">
			<Loader2Icon className="animate-spin" />
		</div>
	);
};
